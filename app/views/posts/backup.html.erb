<!-- our former logic for posts table -->

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @posts.each do |post| %>
  <tr>
  <td style="padding: 0 10px 0 0">
    <% if post.read_status == false%>
      <%= image_tag 'heart.png', height: 10, width: 10 %>
    <% end %>
  </td>
    <td style="padding: 0 10px 0 0">
      <% if post.read_status == false%>
        <strong><%= link_to post.title, post_read_path(post), method: :post %></strong>
      <% else %>
        <%= link_to post.title, post_read_path(post), method: :post %>
      <% end %></td>

    <td style="padding: 0 10px 0 0">
      <% if post.read_status == true %>
          <%= button_to "Mark as unread", post_unread_path(post), class: 'btn btn-xs btn-primary' %>
      <% end %>
    </td>
    </tr>
<% end %>

</table>

<br>
<br>
<%= link_to "All OpenWhens", home_path%>
