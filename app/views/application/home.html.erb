<h1>Welcome, <%=current_user.name.capitalize %>! </h1>

    <!-- <div class="center">
      <a href="/signin">Sign In</a>
      <a href="/users/new">Sign Up</a>
    </div> -->

<% if logged_in?  %>
  <%= button_to "Write a new OpenWhen", new_post_path, :method => "get" %>
    <% if !current_user.posts.empty? %>
        <% current_user_categories.each do |category|   %>
        <%= button_to "Open When... You're #{category.name}", category_posts_path(category.id), :method => "get" %>
        <% end %>
  <% end %>
<% end %>
<p><%= flash[:message] %></p>






  </div>

</div>
    </div>

<!-- current_user.posts.empty? -->
